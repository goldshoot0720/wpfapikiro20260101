<Page x:Class="wpfkiro20260101.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfkiro20260101"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Page.Resources>
        <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366F1" Offset="0"/>
            <GradientStop Color="#8B5CF6" Offset="0.5"/>
            <GradientStop Color="#A855F7" Offset="1"/>
        </LinearGradientBrush>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40" HorizontalAlignment="Center">
                <!-- Logo和標題 -->
                <Border Background="#EF4444" CornerRadius="20" Width="80" Height="80" Margin="0,0,0,30">
                    <TextBlock Text="鋒" FontSize="40" FontWeight="Bold" Foreground="White" 
                             HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                
                <TextBlock Text="鋒兄AI資訊系統" FontSize="32" FontWeight="Bold" Foreground="White" 
                         HorizontalAlignment="Center" Margin="0,0,0,10"/>
                
                <TextBlock Text="智能管理您的影片和圖片收藏，支援智能分類和快速搜尋" 
                         FontSize="16" Foreground="White" Opacity="0.9"
                         HorizontalAlignment="Center" Margin="0,0,0,50"/>
                
                <!-- 版權信息 -->
                <Border Background="#FFFFFF" CornerRadius="15" Padding="40" Margin="0,0,0,40" Opacity="0.95">
                    <StackPanel>
                        <TextBlock Text="鋒兄達習公開資訊 © 版權所有 2025 - 2125" 
                                 FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" 
                                 Foreground="#374151" Margin="0,0,0,30"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 前端技術 -->
                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Text="🔧 前端技術" FontSize="16" FontWeight="Bold" 
                                         Foreground="#F59E0B" Margin="0,0,0,15"/>
                                <TextBlock Text="• SolidJS (SolidStart)" FontSize="14" Foreground="#6B7280" Margin="0,0,0,5"/>
                                <TextBlock Text="• 網頁存放於 Netlify" FontSize="14" Foreground="#6B7280" Margin="0,0,0,5"/>
                                <TextBlock Text="• 響應式設計 + Tailwind CSS" FontSize="14" Foreground="#6B7280"/>
                            </StackPanel>
                            
                            <!-- 後端技術 -->
                            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                                <TextBlock Text="💎 後端技術" FontSize="16" FontWeight="Bold" 
                                         Foreground="#EC4899" Margin="0,0,0,15"/>
                                <TextBlock Text="• Strapi CMS" FontSize="14" Foreground="#6B7280" Margin="0,0,0,5"/>
                                <TextBlock Text="• 多片存放於 Strapi" FontSize="14" Foreground="#6B7280" Margin="0,0,0,5"/>
                                <TextBlock Text="• RESTful API 支援" FontSize="14" Foreground="#6B7280"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 系統功能選單 -->
                <Border Background="#FFFFFF" CornerRadius="15" Padding="30" Opacity="0.95">
                    <StackPanel>
                        <TextBlock Text="🌟 系統功能選單" FontSize="20" FontWeight="Bold" 
                                 HorizontalAlignment="Center" Foreground="#374151" Margin="0,0,0,20"/>
                        
                        <UniformGrid Columns="3" Margin="0,10">
                            <Button Style="{StaticResource {x:Type Button}}" Margin="10" Padding="20" 
                                  Background="#6366F1" Foreground="White" BorderThickness="0" 
                                  Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="🎬" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="影片庫" FontSize="14" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource {x:Type Button}}" Margin="10" Padding="20" 
                                  Background="#10B981" Foreground="White" BorderThickness="0" 
                                  Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="🖼️" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="圖片庫" FontSize="14" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource {x:Type Button}}" Margin="10" Padding="20" 
                                  Background="#F59E0B" Foreground="White" BorderThickness="0" 
                                  Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="📋" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="訂閱管理" FontSize="14" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource {x:Type Button}}" Margin="10" Padding="20" 
                                  Background="#8B5CF6" Foreground="White" BorderThickness="0" 
                                  Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="儀表板" FontSize="14" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource {x:Type Button}}" Margin="10" Padding="20" 
                                  Background="#EF4444" Foreground="White" BorderThickness="0" 
                                  Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="🍎" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="食品管理" FontSize="14" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Style="{StaticResource {x:Type Button}}" Margin="10" Padding="20" 
                                  Background="#6B7280" Foreground="White" BorderThickness="0" 
                                  Cursor="Hand">
                                <StackPanel>
                                    <TextBlock Text="⚙️" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="系統設定" FontSize="14" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>