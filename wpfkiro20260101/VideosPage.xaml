<Page x:Class="wpfkiro20260101.VideosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfkiro20260101"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VideosPage">

    <Page.Resources>
        <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366F1" Offset="0"/>
            <GradientStop Color="#8B5CF6" Offset="0.5"/>
            <GradientStop Color="#A855F7" Offset="1"/>
        </LinearGradientBrush>
        
        <Style x:Key="VideoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- 頁面標題和搜尋 -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Border Background="#8B5CF6" CornerRadius="8" Width="40" Height="40" Margin="0,0,15,0">
                            <TextBlock Text="🎬" FontSize="20" Foreground="White" 
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="鋒兄影片庫" FontSize="24" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="發現的精彩人生與現實世界" FontSize="14" Foreground="White" Opacity="0.8"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Button Grid.Column="2" Content="添加影片" Background="#EF4444" Foreground="White" 
                          BorderThickness="0" Padding="15,8" Cursor="Hand"/>
                </Grid>
                
                <!-- 搜尋欄 -->
                <Border Background="#FFFFFF" CornerRadius="25" Padding="15" Margin="0,0,0,30" Opacity="0.9">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0" Text="搜尋影片名稱..." FontSize="14" BorderThickness="0" 
                               Background="Transparent" VerticalAlignment="Center" Foreground="#6B7280"/>
                        <Button Grid.Column="1" Content="🔍 搜尋" Background="#6366F1" Foreground="White" 
                              BorderThickness="0" Padding="15,8" Cursor="Hand"/>
                    </Grid>
                </Border>
                
                <!-- 影片列表 -->
                <UniformGrid Columns="2" Margin="0,20">
                    <!-- 影片1 -->
                    <Border Style="{StaticResource VideoCardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="200"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 影片縮圖 -->
                            <Border Grid.Row="0" Background="#F3F4F6" CornerRadius="8">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="🎬" FontSize="48" Foreground="#6B7280"/>
                                    <Border Background="#000000" CornerRadius="4" Padding="8,4" Margin="0,10,0,0">
                                        <TextBlock Text="MP4" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <TextBlock Text="00:45" FontSize="12" Foreground="White" 
                                             Background="#000000" Padding="4,2" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 影片信息 -->
                            <StackPanel Grid.Row="1" Margin="15">
                                <TextBlock Text="鋒兄的傳奇人生" FontSize="16" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,5"/>
                                <TextBlock Text="鋒兄人生傳記片段" FontSize="12" Foreground="#6B7280" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="大小: 2.01 MB" FontSize="11" Foreground="#9CA3AF"/>
                                        <TextBlock Text="時長: 00:45" FontSize="11" Foreground="#9CA3AF"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- 影片2 -->
                    <Border Style="{StaticResource VideoCardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="200"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 影片縮圖 -->
                            <Border Grid.Row="0" Background="#000000" CornerRadius="8">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="▶️" FontSize="48" Foreground="White"/>
                                    <Border Background="#000000" CornerRadius="4" Padding="8,4" Margin="0,10,0,0">
                                        <TextBlock Text="MP4" FontSize="12" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <TextBlock Text="01:23" FontSize="12" Foreground="White" 
                                             Background="#000000" Padding="4,2" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 影片信息 -->
                            <StackPanel Grid.Row="1" Margin="15">
                                <TextBlock Text="鋒兄雜貨Show 🔴" FontSize="16" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,5"/>
                                <TextBlock Text="鋒兄雜貨影片精選" FontSize="12" Foreground="#6B7280" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="大小: 4.21 MB" FontSize="11" Foreground="#9CA3AF"/>
                                        <TextBlock Text="時長: 01:23" FontSize="11" Foreground="#9CA3AF"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>