<Page x:Class="wpfkiro20260101.FoodPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfkiro20260101"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FoodPage">

    <Page.Resources>
        <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366F1" Offset="0"/>
            <GradientStop Color="#8B5CF6" Offset="0.5"/>
            <GradientStop Color="#A855F7" Offset="1"/>
        </LinearGradientBrush>
        
        <Style x:Key="FoodCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- 頁面標題和操作 -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Border Background="#10B981" CornerRadius="8" Width="40" Height="40" Margin="0,0,15,0">
                            <TextBlock Text="🍎" FontSize="20" Foreground="White" 
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="食品管理系統" FontSize="24" FontWeight="Bold" Foreground="White"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="管理您的食品存放和保質期訊息" FontSize="14" Foreground="White" Opacity="0.8"/>
                                <TextBlock x:Name="BackendServiceLabel" Text="" FontSize="12" Foreground="#FEF3C7" 
                                         Margin="10,0,0,0" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="添加食品" Background="#EF4444" Foreground="White" 
                              BorderThickness="0" Padding="15,8" Cursor="Hand" Click="AddFood_Click"/>
                        <Button x:Name="RefreshDataButton" Content="🔄 重新載入" Background="#6366F1" Foreground="White" 
                              BorderThickness="0" Padding="15,8" Margin="10,0,0,0" Cursor="Hand" Click="RefreshData_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- 資料來源狀態顯示 -->
                <Border Background="#FFFFFF" CornerRadius="8" Padding="15" Margin="0,0,0,20" Opacity="0.9">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock x:Name="DataSourceInfo" Text="正在載入資料..." FontSize="14" Foreground="#374151"/>
                    </StackPanel>
                </Border>
                
                <!-- 搜尋和批量操作 -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="25" Padding="15" Opacity="0.9">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" Text="搜尋食品名稱或商品..." FontSize="14" BorderThickness="0" 
                                   Background="Transparent" VerticalAlignment="Center" Foreground="#6B7280"/>
                            <Button Grid.Column="1" Content="🔍 搜尋" Background="#6366F1" Foreground="White" 
                                  BorderThickness="0" Padding="15,8" Cursor="Hand"/>
                        </Grid>
                    </Border>
                    
                    <Button Grid.Column="1" Content="📊 重新載入" Background="#10B981" Foreground="White" 
                          BorderThickness="0" Padding="15,8" Margin="10,0,0,0" Cursor="Hand"/>
                </Grid>
                
                <!-- 食品項目列表 -->
                <UniformGrid x:Name="FoodItemsContainer" Columns="2" Margin="0,20">
                    <!-- 動態載入的食品項目將在這裡顯示 -->
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>