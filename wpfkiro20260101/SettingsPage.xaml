<Page x:Class="wpfkiro20260101.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfkiro20260101"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="系統設定">

    <Page.Resources>
        <!-- 卡片樣式 -->
        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 標題樣式 -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- 保存按鈕樣式 -->
        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4F46E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#4338CA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <!-- 頁面標題 -->
            <TextBlock Text="系統設定" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#111827" 
                       Margin="0,0,0,30"/>

            <!-- 後端服務設定 -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <!-- 可點擊的標題區域 -->
                    <Border Background="Transparent" 
                            Cursor="Hand" 
                            MouseLeftButtonDown="BackendServiceHeader_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="BackendExpandIcon" 
                                       Text="▼" 
                                       FontSize="14" 
                                       Foreground="#6B7280" 
                                       Margin="0,0,10,0" 
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="後端服務設定" Style="{StaticResource SectionTitleStyle}" Margin="0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 可折疊的後端服務設定內容 -->
                    <StackPanel x:Name="BackendServiceContent" Margin="0,10,0,0">
                        <TextBlock Text="選擇您要使用的後端服務提供商" 
                                   FontSize="14" 
                                   Foreground="#6B7280" 
                                   Margin="0,0,0,20"/>

                        <!-- Appwrite 選項 -->
                        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
                            <RadioButton x:Name="AppwriteOption" 
                                         Checked="BackendOption_Checked"
                                         Content="Appwrite"
                                         FontWeight="Bold"
                                         Margin="0,0,0,5"/>
                            <TextBlock Text="• 可使用 2GB 容量" 
                                       Foreground="Green" 
                                       FontSize="12" 
                                       Margin="20,0,0,2"/>
                            <TextBlock Text="• 可使用 5GB 流量" 
                                       Foreground="Green" 
                                       FontSize="12" 
                                       Margin="20,0,0,0"/>
                        </StackPanel>

                        <!-- Supabase 選項 -->
                        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
                            <RadioButton x:Name="SupabaseOption" 
                                         Checked="BackendOption_Checked"
                                         Content="Supabase"
                                         FontWeight="Bold"
                                         Margin="0,0,0,5"/>
                            <TextBlock Text="• 可使用 1GB 容量" 
                                       Foreground="Green" 
                                       FontSize="12" 
                                       Margin="20,0,0,2"/>
                            <TextBlock Text="• 可使用 5GB 流量" 
                                       Foreground="Green" 
                                       FontSize="12" 
                                       Margin="20,0,0,0"/>
                        </StackPanel>

                        <!-- NHost 選項 -->
                        <RadioButton x:Name="NHostOption" 
                                     Checked="BackendOption_Checked"
                                     Content="NHost"
                                     Margin="0,0,0,10"/>

                        <!-- Contentful 選項 -->
                        <RadioButton x:Name="ContentfulOption" 
                                     Checked="BackendOption_Checked"
                                     Content="Contentful"
                                     Margin="0,0,0,10"/>

                        <!-- Back4App 選項 -->
                        <RadioButton x:Name="Back4AppOption" 
                                     Checked="BackendOption_Checked"
                                     Content="Back4App"
                                     Margin="0,0,0,10"/>

                        <!-- MySQL 選項 -->
                        <RadioButton x:Name="MySQLOption" 
                                     Checked="BackendOption_Checked"
                                     Content="MySQL"
                                     Margin="0,0,0,10"/>

                        <!-- Strapi 選項 -->
                        <RadioButton x:Name="StrapiOption" 
                                     Checked="BackendOption_Checked"
                                     Content="Strapi"
                                     Margin="0,0,0,10"/>

                        <!-- Sanity 選項 -->
                        <RadioButton x:Name="SanityOption" 
                                     Checked="BackendOption_Checked"
                                     Content="Sanity"
                                     Margin="0,0,0,10"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 連線設定 -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <!-- 可點擊的標題區域 -->
                    <Border Background="Transparent" 
                            Cursor="Hand" 
                            MouseLeftButtonDown="ConnectionSettingsHeader_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="ConnectionExpandIcon" 
                                       Text="▼" 
                                       FontSize="14" 
                                       Foreground="#6B7280" 
                                       Margin="0,0,10,0" 
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="連線設定" Style="{StaticResource SectionTitleStyle}" Margin="0"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 可折疊的連線設定內容 -->
                    <Grid x:Name="ConnectionSettingsContent" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- API URL -->
                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                   x:Name="ApiUrlLabel"
                                   Text="API Endpoint:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"/>
                        <TextBox x:Name="ApiUrlTextBox" 
                                 Grid.Row="0" Grid.Column="1" 
                                 Padding="10" 
                                 Margin="0,0,0,10"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 Background="#F9FAFB"/>

                        <!-- Project ID -->
                        <TextBlock Grid.Row="1" Grid.Column="0" 
                                   x:Name="ProjectIdLabel"
                                   Text="Project ID:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"/>
                        <TextBox x:Name="ProjectIdTextBox" 
                                 Grid.Row="1" Grid.Column="1" 
                                 Padding="10" 
                                 Margin="0,0,0,10"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 Background="#F9FAFB"/>

                        <!-- Database ID (Appwrite 專用) -->
                        <TextBlock Grid.Row="2" Grid.Column="0" 
                                   x:Name="DatabaseIdLabel"
                                   Text="Database ID:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                        <TextBox x:Name="DatabaseIdTextBox" 
                                 Grid.Row="2" Grid.Column="1" 
                                 Padding="10" 
                                 Margin="0,0,0,10"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 Background="#F9FAFB"
                                 Visibility="Collapsed"/>

                        <!-- Bucket ID (Appwrite 專用) -->
                        <TextBlock Grid.Row="3" Grid.Column="0" 
                                   x:Name="BucketIdLabel"
                                   Text="Bucket ID:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                        <TextBox x:Name="BucketIdTextBox" 
                                 Grid.Row="3" Grid.Column="1" 
                                 Padding="10" 
                                 Margin="0,0,0,10"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 Background="#F9FAFB"
                                   Visibility="Collapsed"/>

                        <!-- Food Collection ID (Appwrite 專用) -->
                        <TextBlock Grid.Row="4" Grid.Column="0" 
                                   x:Name="FoodCollectionIdLabel"
                                   Text="Food Table ID:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                        <TextBox x:Name="FoodCollectionIdTextBox" 
                                 Grid.Row="4" Grid.Column="1" 
                                 Padding="10" 
                                 Margin="0,0,0,10"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 Background="#F9FAFB"
                                 Visibility="Collapsed"/>

                        <!-- Subscription Collection ID (Appwrite 專用) -->
                        <TextBlock Grid.Row="5" Grid.Column="0" 
                                   x:Name="SubscriptionCollectionIdLabel"
                                   Text="Subscription Table ID:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                        <TextBox x:Name="SubscriptionCollectionIdTextBox" 
                                 Grid.Row="5" Grid.Column="1" 
                                 Padding="10" 
                                 Margin="0,0,0,10"
                                 BorderBrush="#D1D5DB"
                                 BorderThickness="1"
                                 Background="#F9FAFB"
                                 Visibility="Collapsed"/>

                        <!-- API Key -->
                        <TextBlock Grid.Row="6" Grid.Column="0" 
                                   Text="API Key:" 
                                   FontWeight="Bold" 
                                   Margin="0,0,15,10" 
                                   VerticalAlignment="Center"/>
                        <PasswordBox x:Name="ApiKeyPasswordBox" 
                                     Grid.Row="6" Grid.Column="1" 
                                     Padding="10" 
                                     Margin="0,0,0,10"
                                     BorderBrush="#D1D5DB"
                                     BorderThickness="1"
                                     Background="#F9FAFB"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 資料匯出區域 -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="資料匯出" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="將您的資料匯出為 CSV 格式，方便備份或在其他應用程式中使用" 
                               FontSize="14" 
                               Foreground="#6B7280" 
                               Margin="0,0,0,20"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="DownloadFoodCsvButton" 
                                Content="📥 下載 food.csv" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#059669"
                                Margin="0,0,15,0"
                                Click="DownloadFoodCsv_Click"/>
                        <Button x:Name="DownloadSubscriptionCsvButton" 
                                Content="📥 下載 subscription.csv" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#0891B2"
                                Click="DownloadSubscriptionCsv_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 資料轉換區域 -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="資料轉換" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="將 Appwrite CSV 格式轉換為 Supabase CSV 格式，支援欄位映射和格式調整" 
                               FontSize="14" 
                               Foreground="#6B7280" 
                               Margin="0,0,0,20"/>
                    
                    <!-- 轉換說明 -->
                    <Border Background="#F0F9FF" 
                            BorderBrush="#0EA5E9" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="15" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="📋 轉換說明" 
                                       FontWeight="Bold" 
                                       FontSize="14" 
                                       Foreground="#0369A1" 
                                       Margin="0,0,0,10"/>
                            <TextBlock FontSize="12" Foreground="#0369A1" Margin="0,0,0,5">
                                <Run Text="• Appwrite 格式: "/>
                                <Run Text="$id,name,price,photo,shop,todate,photohash,$createdAt,$updatedAt" FontFamily="Consolas"/>
                            </TextBlock>
                            <TextBlock FontSize="12" Foreground="#0369A1" Margin="0,0,0,5">
                                <Run Text="• Supabase 格式: "/>
                                <Run Text="id,created_at,name,todate,amount,photo,price,shop,photohash" FontFamily="Consolas"/>
                            </TextBlock>
                            <TextBlock Text="• 自動處理欄位映射、日期格式轉換和資料類型調整" 
                                       FontSize="12" 
                                       Foreground="#0369A1"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 轉換操作 -->
                    <StackPanel>
                        <TextBlock Text="選擇 Appwrite CSV 檔案進行轉換：" 
                                   FontWeight="Bold" 
                                   FontSize="14" 
                                   Margin="0,0,0,10"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button x:Name="ConvertFoodCsvButton" 
                                    Content="🔄 轉換 Food CSV" 
                                    Style="{StaticResource SaveButtonStyle}"
                                    Background="#DC2626"
                                    Margin="0,0,15,0"
                                    Click="ConvertFoodCsv_Click"/>
                            <Button x:Name="ConvertSubscriptionCsvButton" 
                                    Content="🔄 轉換 Subscription CSV" 
                                    Style="{StaticResource SaveButtonStyle}"
                                    Background="#7C2D12"
                                    Margin="0,0,15,0"
                                    Click="ConvertSubscriptionCsv_Click"/>
                        </StackPanel>
                        
                        <!-- 批次轉換 -->
                        <Border Background="#FEF3C7" 
                                BorderBrush="#F59E0B" 
                                BorderThickness="1" 
                                CornerRadius="8" 
                                Padding="15" 
                                Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="📁 批次轉換" 
                                           FontWeight="Bold" 
                                           FontSize="14" 
                                           Foreground="#92400E" 
                                           Margin="0,0,0,10"/>
                                <TextBlock Text="選擇包含多個 Appwrite CSV 檔案的資料夾進行批次轉換" 
                                           FontSize="12" 
                                           Foreground="#92400E" 
                                           Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="BatchConvertButton" 
                                            Content="📂 批次轉換資料夾" 
                                            Style="{StaticResource SaveButtonStyle}"
                                            Background="#F59E0B"
                                            Margin="0,0,10,0"
                                            Click="BatchConvert_Click"/>
                                    <Button x:Name="TestConverterButton" 
                                            Content="🧪 測試轉換功能" 
                                            Style="{StaticResource SaveButtonStyle}"
                                            Background="#8B5CF6"
                                            Click="TestConverter_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

                    <!-- 設定檔管理區域 -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="設定檔管理" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="儲存和管理多個設定檔，支援最多100筆設定檔的儲存與載入" 
                               FontSize="14" 
                               Foreground="#6B7280" 
                               Margin="0,0,0,20"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="ManageProfilesButton" 
                                Content="📁 管理設定檔" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#8B5CF6"
                                Margin="0,0,15,0"
                                Click="ManageProfiles_Click"/>
                        
                        <Button x:Name="QuickExportProfilesButton" 
                                Content="📤 快速匯出" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#10B981"
                                Margin="0,0,15,0"
                                Click="QuickExportProfiles_Click"/>
                        
                        <Button x:Name="ExportToFolderButton" 
                                Content="📁 選擇資料夾" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#8B5CF6"
                                Margin="0,0,15,0"
                                Click="ExportToFolder_Click"/>
                        
                        <Button x:Name="TestExportButton" 
                                Content="🧪 測試匯出" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#F59E0B"
                                Margin="0,0,15,0"
                                Click="TestExport_Click"/>
                        
                        <Button x:Name="TestHotReloadButton" 
                                Content="🔥 測試熱重載" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#EF4444"
                                Margin="0,0,15,0"
                                Click="TestHotReload_Click"/>
                        
                        <Button x:Name="QuickTestButton" 
                                Content="⚡ 快速測試" 
                                Style="{StaticResource SaveButtonStyle}"
                                Background="#06B6D4"
                                Margin="0,0,15,0"
                                Click="QuickTest_Click"/>
                        
                        <TextBlock x:Name="ProfileCountInfo" 
                                   Text="載入中..." 
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   Foreground="#6B7280"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 操作按鈕 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ShowSettingsFileButton" 
                        Content="📁 顯示設定檔" 
                        Style="{StaticResource SaveButtonStyle}"
                        Background="#6B7280"
                        Margin="0,0,10,0"
                        Click="ShowSettingsFile_Click"/>
                <Button x:Name="RefreshSettingsButton" 
                        Content="刷新設定" 
                        Style="{StaticResource SaveButtonStyle}"
                        Background="#F59E0B"
                        Margin="0,0,10,0"
                        Click="RefreshSettings_Click"/>
                <Button x:Name="TestSettingsButton" 
                        Content="測試設定" 
                        Style="{StaticResource SaveButtonStyle}"
                        Background="#8B5CF6"
                        Margin="0,0,10,0"
                        Click="TestSettings_Click"/>
                <Button x:Name="TestConnectionButton" 
                        Content="測試連線" 
                        Style="{StaticResource SaveButtonStyle}"
                        Background="#10B981"
                        Margin="0,0,10,0"
                        Click="TestConnection_Click"/>
                <Button x:Name="SaveSettingsButton" 
                        Content="儲存設定" 
                        Style="{StaticResource SaveButtonStyle}"
                        Click="SaveSettings_Click"/>
            </StackPanel>

            <!-- 狀態訊息 -->
            <TextBlock x:Name="StatusMessage" 
                       Margin="0,20,0,0" 
                       FontSize="14" 
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </ScrollViewer>
</Page>