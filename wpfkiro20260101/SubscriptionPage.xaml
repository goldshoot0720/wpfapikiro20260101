<Page x:Class="wpfkiro20260101.SubscriptionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpfkiro20260101"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SubscriptionPage">

    <Page.Resources>
        <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366F1" Offset="0"/>
            <GradientStop Color="#8B5CF6" Offset="0.5"/>
            <GradientStop Color="#A855F7" Offset="1"/>
        </LinearGradientBrush>
        
        <Style x:Key="SubscriptionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- 頁面標題和操作 -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Border Background="#F59E0B" CornerRadius="8" Width="40" Height="40" Margin="0,0,15,0">
                            <TextBlock Text="📋" FontSize="20" Foreground="White" 
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="訂閱管理系統" FontSize="24" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="管理您的各項訂閱服務和會員資訊" FontSize="14" Foreground="White" Opacity="0.8"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Button Grid.Column="2" Content="添加訂閱" Background="#EF4444" Foreground="White" 
                          BorderThickness="0" Padding="15,8" Cursor="Hand"/>
                </Grid>
                
                <!-- 搜尋和批量操作 -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="25" Padding="15" Opacity="0.9">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" Text="搜尋訂閱名稱或網站..." FontSize="14" BorderThickness="0" 
                                   Background="Transparent" VerticalAlignment="Center" Foreground="#6B7280"/>
                            <Button Grid.Column="1" Content="🔍 搜尋" Background="#6366F1" Foreground="White" 
                                  BorderThickness="0" Padding="15,8" Cursor="Hand"/>
                        </Grid>
                    </Border>
                    
                    <Button Grid.Column="1" Content="📊 重新載入" Background="#10B981" Foreground="White" 
                          BorderThickness="0" Padding="15,8" Margin="10,0,0,0" Cursor="Hand"/>
                </Grid>
                
                <!-- 訂閱項目列表 -->
                <StackPanel>
                    <!-- 訂閱項目1 -->
                    <Border Style="{StaticResource SubscriptionCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 服務類型標籤 -->
                            <Border Grid.Column="0" Background="#F59E0B" CornerRadius="6" Padding="8,4" Margin="0,0,15,0">
                                <TextBlock Text="影音娛樂" FontSize="12" FontWeight="Bold" Foreground="White"/>
                            </Border>
                            
                            <!-- 訂閱信息 -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="天虎/實信訊/心臟內科" FontSize="16" FontWeight="Bold" Foreground="#374151"/>
                                <TextBlock Text="網站: https://www.tcmg.com.tw/index.php/main/schedule_time?id=18" 
                                         FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="價格: NT$ 530" FontSize="12" Foreground="#374151" Margin="0,0,15,0"/>
                                    <TextBlock Text="下次付款: 2025-12-26" FontSize="12" Foreground="#374151" Margin="0,0,15,0"/>
                                    <TextBlock Text="剩餘天數: 1 天" FontSize="12" Foreground="#EF4444" FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- 操作按鈕 -->
                            <Button Grid.Column="2" Content="編輯" Background="#6366F1" Foreground="White" 
                                  BorderThickness="0" Padding="12,6" Margin="10,0" Cursor="Hand"/>
                            <Button Grid.Column="3" Content="刪除" Background="#EF4444" Foreground="White" 
                                  BorderThickness="0" Padding="12,6" Cursor="Hand"/>
                        </Grid>
                    </Border>
                    
                    <!-- 訂閱項目2 -->
                    <Border Style="{StaticResource SubscriptionCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 服務類型標籤 -->
                            <Border Grid.Column="0" Background="#10B981" CornerRadius="6" Padding="8,4" Margin="0,0,15,0">
                                <TextBlock Text="軟體" FontSize="12" FontWeight="Bold" Foreground="White"/>
                            </Border>
                            
                            <!-- 訂閱信息 -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="kiro pro" FontSize="16" FontWeight="Bold" Foreground="#374151"/>
                                <TextBlock Text="網站: https://app.kiro.dev/account/" 
                                         FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="價格: NT$ 640" FontSize="12" Foreground="#374151" Margin="0,0,15,0"/>
                                    <TextBlock Text="下次付款: 2026-01-01" FontSize="12" Foreground="#374151" Margin="0,0,15,0"/>
                                    <TextBlock Text="剩餘天數: 10 天" FontSize="12" Foreground="#10B981" FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- 操作按鈕 -->
                            <Button Grid.Column="2" Content="編輯" Background="#6366F1" Foreground="White" 
                                  BorderThickness="0" Padding="12,6" Margin="10,0" Cursor="Hand"/>
                            <Button Grid.Column="3" Content="刪除" Background="#EF4444" Foreground="White" 
                                  BorderThickness="0" Padding="12,6" Cursor="Hand"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>